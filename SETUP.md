# Claude API 集成说明

## 快速开始

### 1. 配置 API 密钥

API 密钥会自动从您本机的 Claude 配置中读取，无需手动设置！

### 2. 启动开发服务器

```bash
# 启动开发服务器（会自动加载 Claude 配置）
npm run dev
```

### 3. 测试功能

1. 在浏览器中打开显示的地址（可能是 http://localhost:5177/ 或其他端口）
2. 在输入框中输入您的问题
3. 点击"发送"按钮
4. 查看智能体的真实响应！

## 我们使用的方案

注意：我们使用**直接 Anthropic API 调用**而不是 `@anthropic-ai/claude-agent-sdk`，因为该 SDK 只支持 Node.js 环境，不能在浏览器中运行。

## 配置加载方式

配置会自动从以下位置读取：
- **Windows**: `C:\Users\您的用户名\.claude\settings.json`

您不需要手动编辑 `.env` 文件，它会在每次运行 `npm run dev` 或 `npm run build` 时自动生成。

## 手动重新加载配置

如果您想手动重新加载 Claude 配置，可以运行：

```bash
npm run config:load
```

## 功能特性

✅ **对话历史管理**
- 支持多种上下文管理策略：完整记忆、滑动窗口、摘要记忆
- 自动保存对话历史

✅ **场景配置**
- 预设多个场景（餐厅预订、研究论文助手、对话分析等）
- 可自定义系统提示词

✅ **实时响应**
- 直接与 Claude API 通信
- 显示加载状态
- 错误处理和用户友好的错误消息

## 上下文管理策略

- **完整记忆**：保留所有对话历史（默认）
- **滑动窗口**：只保留最近的消息（最多 10 条）
- **摘要记忆**：对话摘要（可扩展功能）

## 故障排查

### 问题 1：看到 "Please configure your Claude API key" 错误

**解决方案**：
- 确保您已经安装并配置了 Claude Code
- 检查 `C:\Users\您的用户名\.claude\settings.json` 文件是否存在
- 运行 `npm run config:load` 手动重新加载配置

### 问题 2：按钮点击没有反应

**解决方案**：
- 打开浏览器开发者工具（按 F12）
- 查看 Console 标签页中的错误信息
- 确保开发服务器正在运行

### 问题 3：发送消息后显示网络错误

**解决方案**：
- 检查网络连接是否正常
- 确认您的 API 账户有足够的额度
- 查看 Console 中的详细错误信息
- 确认没有防火墙或代理阻止 API 调用

### 问题 4：看到 "Failed to fetch" 或 CORS 错误

**解决方案**：
- 这可能是 CORS 政策问题
- 可以考虑使用代理服务器
- 或者检查浏览器控制台中的具体错误信息

## 下一步

现在您已经成功集成了 Claude API！接下来可以尝试：

1. 切换不同的场景（餐厅预订、研究论文助手等）
2. 修改系统提示词来定制智能体的行为
3. 选择不同的工具配置
4. 调整上下文管理策略
5. 查看 Token 使用情况（即将推出）

享受您的智能体上下文管理实验平台！🚀
