# RQ-060 会话任务选择后自动收缩

## 背景

会话任务浮层用于快速跳转用户任务。当前选择任务后面板仍保持展开，会遮挡当前聊天视野。

## 目标

- 用户点击任务列表中的任一任务后，执行跳转并自动收起任务面板。
- 顶部“任务 N”入口保持可见，可再次展开。

## 范围

- 仅修改 `SessionTaskNavigator` 交互。
- 不改变任务派生规则、任务样式、滚动定位和空态行为。

## 验收标准

- 展开任务列表后点击任务项，会调用 `onJumpToMessage`。
- 点击后任务面板消失，入口按钮 `aria-expanded` 变回 `false`。
