# 手机模式隐藏左右侧栏设计

目标：在手机精简模式下进一步隐藏 Agent Runtime 左侧应用库、右侧助手侧栏、相邻水平拖拽条、底部观测栏及其垂直拖拽条，把空间留给中间对话工作区。

范围：复用现有 `mobile-compact-hidden` / `body.mobile-compact` 机制；桌面端布局不变；不新增移动端抽屉入口。

验收：手机 Docker 页面中左右两侧和底部状态栏不占空间，中间对话区铺满；桌面端仍显示左右侧栏和底部观测栏。